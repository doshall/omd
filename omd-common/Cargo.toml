[package]
name = "omd-common"
version = "0.5.0"
edition = "2021"
description = "Shared Markdown parsing for omd"
license = "MIT"

[dependencies]
pulldown-cmark = { version = "0.12", features = ["html"] }
serde = { version = "1", features = ["derive"] }
